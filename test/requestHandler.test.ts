import 'jest';

describe('RequestHandler Test', () => {
    describe('#handleRequest', () => {
        describe('when invoked with config for a request for resource', () => {
            it('should invoke action of asked controller', () => {
                // TODO
            });
        });
    });
});
