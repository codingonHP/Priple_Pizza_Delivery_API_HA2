import 'jest';

describe('AppRouter Test', () => {
    describe('#registerRoutes', () => {
        describe('given a collection of routes', () => {
            it('should save all the routes', () => {
                // TODO
            });
        });
    });
});
