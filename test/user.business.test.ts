import 'jest';

describe('UserBusiness Test', () => {
    describe('#saveUserAsync', () => {
        describe('when called with user', () => {
            it('should save user object', () => {
                // TODO
            });
        });
    });
});
